# 오늘 한 걸음 V9.1 UX·콘텐츠 최종 점검

기준: V9.0.20 안정판 / `main` 기준

목적: 새 기능을 더 붙이기 전에 메뉴 복잡성, 저장·기록 흐름, 일정·알림 관리, 기능 중복, 도움말 정합성을 점검하고 V9.1의 변경 범위를 최소화한다.

## 1. 결론

현재 정보구조는 큰 틀에서 유지하는 것이 맞다.

- 하단 핵심 메뉴는 `홈 · 회복도구 · 헬프 · 마음프로 · 커뮤니티` 5개를 유지한다.
- `나`는 홈 우측 상단의 사람 버튼으로 들어가는 현재 구조를 유지한다.
- 회복도구는 `내 계획 · 배우기 · 실천하기 · 자가점검` 4구획을 유지한다.
- 일정은 `회복도구 → 내 계획 → 일정 · 알림`을 단일 허브로 유지한다.
- 습관·생활·치료·알림을 다시 각각 상위 메뉴로 분리하지 않는다.
- 새 기능 때문에 하단 탭이나 회복도구 카드 수를 계속 늘리지 않는다. 가능하면 기존 허브 안에 넣는다.

즉 V9.1의 방향은 **새 메뉴 추가가 아니라 기록 조회와 명칭의 일관성을 높이는 소규모 통합**이 적절하다.

## 2. 현재 구조에서 잘 유지해야 할 부분

### 2.1 일정 · 알림 단일 허브

현재 `일정 · 알림` 안에서 다음을 한 흐름으로 관리한다.

- 습관
- 생활 일정: 위험시간 · 식사 · 잠
- 치료 일정: 복약 · 처방 · 외래 · 반복 외래
- 알림 설정

홈의 `오늘 일정`은 별도 설정체계를 만들지 않고 `일정 관리`를 통해 같은 허브로 연결된다.

이 구조는 과거의 치료관리·챙기기 기능을 각각 별도 메뉴로 두는 것보다 단순하며, 앞으로 모임 일정 등을 추가하더라도 동일한 관리 원칙을 적용할 수 있다.

### 2.2 회복도구의 목적별 구분

현재 회복도구는 기능명을 한 줄로 나열하지 않고 다음 네 목적에 따라 나뉜다.

1. 내 계획
2. 배우기
3. 실천하기
4. 자가점검

`배우기`는 3열 카드, `실천하기`도 작은 3열 카드 중심으로 구성되어 있어 현재 수준에서는 메뉴 밀도가 과도하지 않다.

### 2.3 내 발자취를 기록 조회 허브로 사용하는 원칙

`나 → 내 발자취`는 다음 기록을 한곳에서 다시 보게 하는 구조다.

- 감정
- 충동
- 하루
- 몸
- 다시 시작
- 실천기록
- 자가점검
- 통계 · 패턴

12단계 기록과 회복 실천도구 기록은 이미 `실천기록` 안에서 필터링하여 다시 볼 수 있다. 이 원칙을 V9.1에서도 유지한다.

## 3. V9.1에서 수정할 우선 항목

### 우선순위 A — 명칭 충돌 해소

현재 회복도구 한 화면에 다음 두 표현이 같이 존재한다.

- 실천하기: `의미점검 보기`
- 자가점검: `점검 보기`

둘 다 '점검'과 '보기'를 사용하여 서로 다른 기능임에도 구분이 약하다.

V9.1 권장 명칭:

- `의미점검 보기` → **`의미점검`**
- 자가점검의 `점검 보기` → **`자가점검 기록`**

경로와 저장구조는 바꾸지 않는다.

이 변경 시 FAQ·사용설명서의 `의미점검 보기` 표현도 동일하게 동기화한다.

### 우선순위 A — 의미 관련 기록을 내 발자취에 연결

현재 의미 관련 기록은 자체 화면에서는 잘 조회되지만 `내 발자취 → 실천기록`에는 포함되지 않는다.

현재 저장원본은 그대로 사용한다.

- 의미 돌아보기: 기존 `S.wbDays`
- 의미회복 간편점검 완료기록: 기존 `S.meaningChecks`

V9.1에서는 새 저장키나 데이터 복사를 만들지 않고, `내 발자취 → 실천기록`이 위 기존 데이터를 **읽어서 함께 표시**하도록 한다.

권장 필터:

- 전체
- 의미
- 12단계
- 실천도구
- 가족모드에서는 기존 가족도구 필터 유지

새로운 `의미` 탭을 내 발자취 최상위에 추가하지 않는다. 최상위 탭이 더 늘면 기록 허브가 다시 복잡해진다.

표시 권장:

- 의미 돌아보기: 날짜 + 오늘의 문장 또는 짧은 요약
- 의미회복 간편점검: 날짜 + 총점/40 + 4개 영역 요약
- 상세보기는 기존 의미 화면/기록 상세를 재사용

삭제나 수정은 기존 원본 기능에서 처리하고, 내 발자취는 우선 조회 중심으로 둔다.

### 우선순위 B — 기록 흐름 문구 통일

사용자에게 다음 원칙이 일관되게 보이도록 한다.

- **새 기록·새 연습 시작**: 회복도구
- **오늘 챙길 일정 확인**: 홈
- **일정 설정·알림 관리**: 일정 · 알림
- **지난 기록·변화 확인**: 나 → 내 발자취

기능별 화면에 `내 기록 보기` 바로가기가 있어도 최종 기록 허브는 `내 발자취`로 통일한다.

의미 돌아보기 화면의 자체 `지난 기록`은 오늘 기록을 이어서 살펴보는 기능으로 유지하되, 전체 기록 조회에서는 내 발자취에서도 접근 가능하게 한다.

### 우선순위 B — 모임 일정 추가 원칙

향후 AA/NA/GA 등 모임 일정을 사용자가 직접 관리하는 기능을 추가한다면 별도 하단 메뉴나 별도 '모임관리' 허브를 만들지 않는다.

권장 위치:

`회복도구 → 내 계획 → 일정 · 알림`

권장 방식:

- 기존 일정 데이터·알림 예약 방식과 충돌하지 않도록 별도 일정 종류로 추가
- 홈의 `오늘 일정`에서 같은 형태로 표시
- Android 예약알림은 기존 네이티브 알림 엔진을 재사용하고 엔진 자체는 변경하지 않음

실제 추가는 별도 기능 단계로 진행하며 V9.1 기본 정리와 동시에 무리하게 넣지 않는다.

## 4. 이번 점검에서 하지 않을 것

다음은 V9.1 UX 정리와 무관하므로 변경하지 않는다.

- `DATA_SCHEMA = 6`
- 개인 저장키 `ohg.v1`
- 소셜 저장키 `ohg.social.v1`
- `SOCIAL_VERSION`
- Android exact alarm / boot rescheduling
- `setExactAndAllowWhileIdle`
- 치료 알림 `scheduleNextForOffset`
- Relax TTS
- MindPro Voice TTS
- 현재 V9.0.20 APK/AAB/Release/tag
- 하단 메뉴 개수와 순서
- 회복도구의 4개 큰 구획

## 5. V9.1 최소 변경안

V9.1 첫 작업은 아래 세 가지로 제한하는 것이 적절하다.

1. 회복도구 명칭 정리
   - `의미점검 보기` → `의미점검`
   - `점검 보기` → `자가점검 기록`

2. `내 발자취 → 실천기록`에 기존 의미 기록 연결
   - 새 저장키 없음
   - 데이터 복사 없음
   - `S.wbDays`, `S.meaningChecks`를 읽어 표시
   - `의미` 필터 추가

3. FAQ·사용설명서 동기화
   - 메뉴 이름
   - 새 기록은 회복도구 / 지난 기록은 내 발자취라는 흐름

이 세 작업이 끝난 뒤 전체 회귀검증을 통과하면 그때 V9.1 버전 승격을 판단한다.

## 6. 완료 판정 기준

V9.1 UX 정리는 다음 조건을 모두 만족해야 완료로 본다.

- 새 하단 메뉴가 추가되지 않음
- 일정·알림 허브가 유지됨
- 의미점검과 자가점검 기록 버튼이 이름만 보고 구분됨
- 의미 돌아보기와 의미회복 간편점검의 기존 완료기록이 내 발자취에서도 조회됨
- 기존 기록을 복제하거나 이동하지 않음
- 기존 의미 화면의 지난 기록 기능도 정상 유지됨
- 12단계·SMART 실천기록 필터 정상 유지
- 가족모드에서 당사자 의미기록이 노출되지 않음
- `DATA_SCHEMA=6`, `ohg.v1`, `ohg.social.v1` 유지
- JS syntax / no-undef / Playwright 회귀검증 통과
- Android 알림/TTS 관련 파일과 릴리스 산출물 변경 없음
